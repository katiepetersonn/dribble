
<%= form_for([@project, @project.comments.build]) do |f| %>
  <!-- <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p> -->
  <p>
    <%= f.label :post %><br>
    <%= f.text_area :post %>
  </p>
  <p>
      <button class="btn waves-effect waves-light #f06292 pink lighten-2" type="submit" name="action">Submit</button>
  </p>
<% end %>

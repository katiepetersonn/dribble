<h1>All Projects</h1>

<div class="container">
  <div class="row">
      <% @all_projects.each do |project| %>
          <!-- <div class="col s6">
            <%= image_tag project.image %>
          </div> -->
          <div class="col s6">
            <%= link_to(project.title, "/projects/#{project.id}") %>
          </div>
      <% end %>
  </div>
</div>
